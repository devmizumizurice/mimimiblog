/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.microcms-assets.io', 'lh3.googleusercontent.com', 's.gravatar.com']
  }
}

module.exports = nextConfig
